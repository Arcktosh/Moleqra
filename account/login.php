<?php
$rootPrefix='../';$pageTitle='Sign in | Moleqra';$pageRobots='noindex,nofollow';require_once __DIR__.'/../includes/bootstrap.php';require_once __DIR__.'/../includes/account_auth.php';$error='';if(customer_user()){header('Location: index.php');exit;}
if($_SERVER['REQUEST_METHOD']==='POST'){
    if(!csrf_valid($_POST['csrf_token']??null))$error='Your session expired.';
    else{$result=customer_authenticate((string)($_POST['email']??''),(string)($_POST['password']??''));if($result['ok']){header('Location: index.php');exit;}$error=$result['reason']==='unverified'?'Please verify your email address before signing in.':'Email or password is incorrect.';}
}
require __DIR__.'/../includes/header.php';?>
<section class="page-hero"><div class="container"><div class="eyebrow">Customer account</div><h1>Sign in</h1></div></section><section class="section"><div class="container narrow"><?php if(isset($_GET['verify_required'])):?><div class="alert success">Account created. Verify your email before signing in.</div><?php endif;?><?php if(isset($_GET['reset'])):?><div class="alert success">Password updated. You can now sign in.</div><?php endif;?><?php if(isset($_GET['verified'])):?><div class="alert success">Email verified.</div><?php endif;?><?php if($error):?><div class="alert error"><?= e($error) ?></div><?php endif;?><form method="post" class="card"><input type="hidden" name="csrf_token" value="<?= e(csrf_token()) ?>"><div class="field"><label>Email</label><input type="email" name="email" required autocomplete="email"></div><div class="field"><label>Password</label><input type="password" name="password" required autocomplete="current-password"></div><button class="btn primary">Sign in</button></form><p><a href="forgot-password.php">Forgot password?</a> · <a href="resend-verification.php">Resend verification</a> · Need an account? <a href="register.php">Register</a>.</p></div></section><?php require __DIR__.'/../includes/footer.php';?>
