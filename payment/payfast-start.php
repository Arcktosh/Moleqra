<?php
require_once __DIR__.'/../includes/bootstrap.php';require_once __DIR__.'/../includes/payments.php';$pdo=db();$token=(string)($_GET['token']??'');if(!$pdo||!commerce_schema_ready($pdo)){http_response_code(503);exit('Checkout unavailable.');}$order=commerce_order_by_token($pdo,$token);if(!$order){http_response_code(404);exit('Order not found.');}if($order['payment_status']==='Paid'){header('Location: ../order.php?token='.$token);exit;}if(!payfast_configured()){http_response_code(503);exit('PayFast is not configured yet. Copy config/payment.example.php to config/payment.php and enter sandbox or live credentials.');}$data=payfast_checkout_data($order);
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Redirecting to PayFast</title></head><body><form id="pf" action="<?= e(payfast_process_url()) ?>" method="post"><?php foreach($data as $k=>$v):?><input type="hidden" name="<?= e($k) ?>" value="<?= e((string)$v) ?>"><?php endforeach;?><noscript><button type="submit">Continue to PayFast</button></noscript></form><script>document.getElementById('pf').submit();</script></body></html>
